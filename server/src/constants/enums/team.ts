export enum TeamRole {
  OWNER = 'owner',
  LEAD = 'lead',
  CONTRIBUTOR = 'contributor',
  VIEWER = 'viewer'
}

export enum TeamStatus {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
  PENDING = 'pending'
}